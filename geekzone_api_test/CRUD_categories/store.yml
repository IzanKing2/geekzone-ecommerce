info:
  name: store
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/categorias
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
