info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/register
  body:
    type: json
    data: |-
      {
        "name": "Prueba",
        "email": "prueba@geekzone.com",
        "password": "prueba123",
        "password_confirmation": "prueba123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
